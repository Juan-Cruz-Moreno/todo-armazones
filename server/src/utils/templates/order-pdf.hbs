<html>
  <head>
    <meta charset="utf-8" />
    <title>Orden #{{orderNumber}}</title>
    <style>
      body { font-family: 'Segoe UI', Arial, sans-serif; background: #fff;
      color: #111; margin: 0; padding: 0; padding-bottom: 80px; } .header {
      background: #fff; border-bottom: 1px solid #e0e0e0; padding: 24px 24px
      16px 24px; margin: 16px 40px 0 40px; text-align: center; } .logo { height:
      100px; width: 120px; margin-left: 20px; background: none; } .sub-header {
      display: flex; justify-content: space-between; align-items: center;
      margin: 0; text-align: left; } .sub-header div { flex: 1; } .order-info {
      background: #fff; margin: 32px 40px 0 40px; padding: 24px 24px 16px 24px;
      } .order-label { font-size: 1rem; color: #222; font-weight: 500;
      letter-spacing: 1px; } .order-number { font-size: 2rem; font-weight: bold;
      color: #000; margin-bottom: 8px; letter-spacing: 1px; } .meta {
      margin-bottom: 8px; color: #222; } .section-title { font-size: 1rem;
      color: #111; margin-top: 24px; margin-bottom: 8px; font-weight: 600;
      letter-spacing: 0.5px; } /* === Nueva tabla más "user friendly" === */
      table { width: 100%; border-collapse: separate; border-spacing: 0 8px;
      margin-top: 16px; margin-bottom: 40px; } th { background: #9797aa; color:
      #fff; font-weight: 600; padding: 12px 10px; text-align: left; }
      th:nth-child(2), th:nth-child(3), th:nth-child(4) { text-align: center; }
      td { background: #fff; padding: 12px 10px; border: none; box-shadow: none;
      } tr td:first-child { border-top-left-radius: 0;
      border-bottom-left-radius: 0; } tr td:last-child {
      border-top-right-radius: 0; border-bottom-right-radius: 0; }
      td:nth-child(2), td:nth-child(3), td:nth-child(4) { text-align: center;
      font-variant-numeric: tabular-nums; } .total-row td { background: #f5f5f5;
      font-weight: bold; font-size: 1.05rem; color: #000; box-shadow: none; }
      .partial-total-row td { background: #e8e8e8; font-weight: 600; font-size:
      0.95rem; color: #333; box-shadow: none; font-style: italic; }
      .total-ars-row td { background: #f5f5f5; font-weight: bold; font-size:
      1.05rem; color: #555; box-shadow: none; } tr { page-break-inside: avoid; }
    </style>
  </head>
  <body>
    <div class="header">
      <h1 style="margin: 0; font-size: 1.5rem; text-align: left;">REMITO</h1>
      <div class="sub-header">
        <div style="text-align: left;">
          <strong>TODO ARMAZONES</strong><br />
          Buenos Aires, Argentina
        </div>
        <div style="text-align: right;">
          <img src="{{logoUrl}}" class="logo" alt="Logo TODO ARMAZONES" />
        </div>
      </div>
    </div>

    <div class="order-info">
      <div class="order-label">Orden</div>
      <div class="order-number">#{{orderNumber}}</div>
      <div class="meta"><strong>Fecha:</strong> {{createdAt}}</div>

      <!-- Contenedor de dos columnas -->
      <div style="display: flex; gap: 40px; margin-top: 24px;">
        <!-- Columna izquierda -->
        <div
          style="flex: 1; border-right: 1px dotted #e0e0e0; padding-right: 20px; position: relative;"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
            stroke-width="1.5"
            stroke="currentColor"
            style="width: 24px; height: 24px; position: absolute; right: -12px; top: 50%; transform: translateY(-50%) rotate(90deg); color: #e0e0e0;"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="m7.848 8.25 1.536.887M7.848 8.25a3 3 0 1 1-5.196-3 3 3 0 0 1 5.196 3Zm1.536.887a2.165 2.165 0 0 1 1.083 1.839c.005.351.054.695.14 1.024M9.384 9.137l2.077 1.199M7.848 15.75l1.536-.887m-1.536.887a3 3 0 1 1-5.196 3 3 3 0 0 1 5.196-3Zm1.536-.887a2.165 2.165 0 0 0 1.083-1.838c.005-.352.054-.695.14-1.025m-1.223 2.863 2.077-1.199m0-3.328a4.323 4.323 0 0 1 2.068-1.379l5.325-1.628a4.5 4.5 0 0 1 2.48-.044l.803.215-7.794 4.5m-2.882-1.664A4.33 4.33 0 0 0 10.607 12m3.736 0 7.794 4.5-.802.215a4.5 4.5 0 0 1-2.48-.043l-5.326-1.629a4.324 4.324 0 0 1-2.068-1.379M14.343 12l-2.882 1.664"
            />
          </svg>
          <div class="section-title">Información del Cliente:</div>
          <div class="meta"><strong>Nombre:</strong>
            {{user.firstName}}
            {{user.lastName}}</div>
          <div class="meta"><strong>Email:</strong> {{user.email}}</div>
          <div class="meta"><strong>Teléfono:</strong> {{user.phone}}</div>
          <div class="meta"><strong>DNI:</strong> {{user.dni}}</div>

          <div class="section-title">Dirección de Envío</div>
          <div class="meta">
            {{shippingAddress.streetAddress}},
            {{shippingAddress.city}},
            {{shippingAddress.state}},
            {{shippingAddress.postalCode}}
          </div>
          <div class="meta"><strong>Tel:</strong>
            {{shippingAddress.phoneNumber}}
            |
            <strong>DNI:</strong>
            {{shippingAddress.dni}}
          </div>
          <div class="meta"><strong>Método de envío:</strong>
            {{shippingMethodLabel}}</div>

          {{#if isParcelCompany}}
            {{#if shippingAddress.shippingCompany}}
              <div class="meta"><strong>Empresa de paquetería:</strong>
                {{shippingAddress.shippingCompany}}</div>
            {{/if}}
            {{#if shippingAddress.declaredShippingAmount}}
              <div class="meta"><strong>Monto declarado envío:</strong>
                {{shippingAddress.declaredShippingAmount}}</div>
            {{/if}}
          {{/if}}

          {{#if isMotorcycle}}
            {{#if shippingAddress.deliveryWindow}}
              <div class="meta"><strong>Franja horaria de entrega:</strong>
                {{shippingAddress.deliveryWindow}}</div>
            {{/if}}
          {{/if}}

          <div class="meta"><strong>Método de pago:</strong>
            {{paymentMethodLabel}}</div>

          <!-- Total en pesos (ARS) mostrado en la columna izquierda -->
          {{#if totalAmountARS}}
            <!-- Separador visual para dar más preponderancia al total -->
            <div style="margin-top: 18px; padding-top: 12px; border-top: 1px solid #e0e0e0;">
              <div class="meta" style="font-weight: 700; font-size: 1.05rem; color: #000; background: transparent; padding: 4px 0;">
                <strong>Total ARS:</strong>
                <span style="margin-left:8px;">{{totalAmountARS}}</span>
              </div>
              <!-- Leyenda informativa sobre el tipo de cambio (color ligeramente más oscuro) -->
              <div class="meta" style="font-size:0.85rem; color:#555; margin-top:8px;">
                Total en pesos sujeto a tipo de cambio al día de pago
              </div>
            </div>
          {{/if}}
        </div>

        <!-- Columna derecha (duplicada) -->
        <div style="flex: 1; padding-left: 20px; margin-bottom: 40px;">
          <div class="section-title">ETIQUETA DE DESPACHO:</div>
          <div class="meta"><strong>Nombre:</strong>
            {{user.firstName}}
            {{user.lastName}}</div>
          <div class="meta"><strong>Email:</strong> {{user.email}}</div>
          <div class="meta"><strong>Teléfono:</strong> {{user.phone}}</div>
          <div class="meta"><strong>DNI:</strong> {{user.dni}}</div>

          <div class="section-title">Dirección de Envío</div>
          <div class="meta">
            {{shippingAddress.streetAddress}},
            {{shippingAddress.city}},
            {{shippingAddress.state}},
            {{shippingAddress.postalCode}}
          </div>
          <div class="meta"><strong>Tel:</strong>
            {{shippingAddress.phoneNumber}}
            |
            <strong>DNI:</strong>
            {{shippingAddress.dni}}
          </div>
          <div class="meta"><strong>Método de envío:</strong>
            {{shippingMethodLabel}}</div>

          {{#if isParcelCompany}}
            {{#if shippingAddress.shippingCompany}}
              <div class="meta"><strong>Empresa de paquetería:</strong>
                {{shippingAddress.shippingCompany}}</div>
            {{/if}}
            {{#if shippingAddress.declaredShippingAmount}}
              <div class="meta"><strong>Monto declarado envío:</strong>
                {{shippingAddress.declaredShippingAmount}}</div>
            {{/if}}
          {{/if}}

          {{#if isMotorcycle}}
            {{#if shippingAddress.deliveryWindow}}
              <div class="meta"><strong>Franja horaria de entrega:</strong>
                {{shippingAddress.deliveryWindow}}</div>
            {{/if}}
          {{/if}}
        </div>
      </div>
    </div>

    <!-- Nueva página para la tabla de items -->
    <div style="page-break-before: always;"></div>

    <div class="order-info" style="margin-top: 0; padding-top: 0;">
      {{#each itemPages}}
        <table style="margin-top: 0;{{#if @last}} margin-bottom: 0;{{/if}}">
          <thead>
            <tr>
              <th>Producto</th>
              <th>Cantidad</th>
              <th>Precio USD</th>
              <th>Subtotal USD</th>
            </tr>
          </thead>
          <tbody>
            {{#each items}}
              <tr>
                <td>
                  <div>
                    <span>{{productVariant.product.productModel}}</span>
                  </div>
                  <div style="margin-top: 4px;">
                    <span>{{productVariant.colorName}}</span>
                  </div>
                </td>
                <td>{{quantity}}</td>
                <td>{{priceUSDAtPurchase}}</td>
                <td>{{subTotal}}</td>
              </tr>
            {{/each}}

            <!-- Total de unidades en esta página -->
            <tr class="partial-total-row">
              <td>Unidades en esta página: {{pageItemsCount}}</td>
              <td></td>
              <td></td>
              <td></td>
            </tr>
          </tbody>
        </table>

        {{#unless @last}}
          <!-- Salto de página entre tablas -->
          <div style="page-break-before: always;"></div>
        {{/unless}}

      {{/each}}

      <!-- Total general de unidades -->
      <table style="margin-top: 0; margin-bottom: 0;">
        <tbody>
          <tr class="total-row">
            <td><strong>Total de unidades: {{itemsCount}}</strong></td>
            <td></td>
            <td></td>
            <td></td>
          </tr>

          {{#if refund}}
            {{#if refund.originalSubTotal}}
              <!-- Subtotal original tachado -->
              <tr class="total-row">
                <td colspan="3" style="text-align:right;">Subtotal original USD:</td>
                <td style="text-decoration: line-through;">{{formatCurrency
                    refund.originalSubTotal
                    "en-US"
                    "USD"
                  }}</td>
              </tr>
            {{/if}}
            <!-- Descuento en rojo -->
            <tr class="total-row">
              <td colspan="2" style="text-align:left; color: red;">{{#if
                  refund.reason
                }}Motivo: {{refund.reason}}{{/if}}</td>
              <td style="text-align:right; color: red;">Descuento USD
                {{#if refund.type}}{{refundDescription}}{{/if}}:</td>
              <td style="color: red;">-{{formatCurrency
                  refund.appliedAmount
                  "en-US"
                  "USD"
                }}</td>
            </tr>
          {{/if}}

          {{#if showSubTotal}}
          <!-- Subtotal general -->
          <tr class="total-row">
            <td colspan="3" style="text-align:right;">Subtotal USD:</td>
            <td>{{subTotal}}</td>
          </tr>
          {{/if}}

          {{#if showBankTransferExpense}}
            <tr class="total-row">
              <td colspan="3" style="text-align:right;">Gasto por transferencia
                bancaria USD:</td>
              <td>{{bankTransferExpense}}</td>
            </tr>
          {{/if}}

          <tr class="total-row">
            <td colspan="3" style="text-align:right;">Total USD:</td>
            <td>{{totalAmount}}</td>
          </tr>

          <tr class="total-ars-row">
            <td style="text-align:left;">Tipo de cambio: {{exchangeRate}}</td>
            <td colspan="2" style="text-align:right;">Total ARS:</td>
            <td>{{totalAmountARS}}</td>
          </tr>

        </tbody>
      </table>

      {{#if comments}}
        <div class="section-title">Comentarios:</div>
        <div class="meta">{{comments}}</div>
      {{/if}}

      <!-- Información adicional debajo de los totales -->
      <div
        style="font-size: 14px; color: #333; text-align: center; border: 2px solid #e0e0e0; padding: 15px 12px; line-height: 1.8; background: #f9f9f9;"
      >
        <div style="font-weight: 500;">• Pagos por transferencia o depósito
          bancario incluyen un 4% de interés sobre el Total</div>
        <div style="font-weight: 500;">• Costos extra de envío a cargo del
          Cliente</div>
        <div style="font-weight: 500;">• Los precios y totales en pesos son
          estimativos y sujetos a modificación al tipo de cambio del día de pago
        </div>
      </div>
    </div>
  </body>
</html>